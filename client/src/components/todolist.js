import React from 'react'

    const Todolist = ({ todolist }) => {
      return (
        <div>
          <center><h1>{todolist.title}</h1></center>
        </div>
      )
    };

    export default Todolist